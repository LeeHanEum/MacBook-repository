#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main (){

    double w, h, area, perimeter;

    printf("가로 세로 순서대로 입력 : ");
    scanf("%lf %lf", &w, &h);

    printf("%lf %lf", w, h);

    return 0;
}